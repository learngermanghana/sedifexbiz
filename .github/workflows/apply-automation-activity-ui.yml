name: Apply automation activity UI patch

on:
  push:
    branches:
      - feature/automation-center
    paths:
      - '.github/workflows/apply-automation-activity-ui.yml'
      - 'scripts/patch_automation_activity_ui.py'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/automation-center
      - name: Patch activity query
        run: python scripts/patch_automation_activity_ui.py
      - name: Commit activity query
        run: |
          if git diff --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add web/src/pages/AutomationCenter.tsx
          git commit -m 'fix: use store-scoped automation activity feed'
          git push origin HEAD:feature/automation-center
